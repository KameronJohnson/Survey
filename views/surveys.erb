<h1> <%= @survey.survey_name() %> </h1>

<p> Add questions to this survey </p>

<form action='/questions' method='post'>
  <input id ='survey_id' name='survey_id' type='hidden' value='<%= @survey.id() %>'>
  <label for='the_question'> Question: </label>
  <input id='the_question' name='the_question' type='text'>

  <button type="submit">Add Question</button>
</form>

<% if @survey.questions().any?() %>
<p> Here are the questions in this survey </p>

<ul>
  <% @survey.questions().each() do |question| %>
  <li>
    <%= question.the_question() %>
  </li>
  <% end %>
</ul>

<% else %>
  <p>Well, why don't you ask a question?</p>
<% end %>

<a href='/'>Back to Home Page</a>
